{
  "mcpServers": {
    "fusion": {
      "command": "uv",
      "args": ["run", "--directory", "${workspaceFolder}", "fusion", "mcp"],
      "env": {
        "FUSION_DB_PATH": "${workspaceFolder}/fusion_runs.db"
      }
    }
  }
}
